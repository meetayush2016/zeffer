apiVersion: v1
kind: Service
metadata:
  name: product-service   # THIS NAME IS CRITICAL

spec:
  selector:
    app: product

  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002

  type: ClusterIP